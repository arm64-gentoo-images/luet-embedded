install:
- bash /live_setup.sh || true
# eselect-bzimage is not present and i think link manually.
# PRE: we always have only one kernel.
- ln -s /boot/initramfs-* /boot/Initrd
- ln -s /boot/kernel-genkernel-* /boot/bzImage
